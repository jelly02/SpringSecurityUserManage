<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.SpringSecurity.sample.mapper.MemberMapper">
    <select id="getMemberInfo" parameterType="String" resultType="com.SpringSecurity.sample.vo.Member">
        SELECT
            id, password, name,phone, role, education, isMajor
        FROM member
        WHERE id = #{id}
    </select>

    <insert id="insertMember" parameterType="com.SpringSecurity.sample.vo.Member">
        INSERT INTO member (id, password, name, phone, role, education, isMajor)
        VALUES (#{id}, #{password}, #{name}, #{phone}, #{role}, #{education}, #{isMajor})
    </insert>
</mapper>